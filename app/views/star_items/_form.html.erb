<%= form_for(@star_item) do |f| %>
  <% if @star_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@star_item.errors.count, "error") %> prohibited this star_item from being saved:</h2>

      <ul>
      <% @star_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
